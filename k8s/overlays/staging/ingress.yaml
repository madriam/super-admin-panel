apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: admin-panel
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - admin.ilhaperdida.com.br
      secretName: admin-panel-tls
  rules:
    - host: admin.ilhaperdida.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin-panel
                port:
                  number: 80
