/**
 * Firebase stub - authentication disabled for initial deployment
 * TODO: Implement proper authentication when needed
 */

// Stub exports to prevent import errors
export const app = null;
export const auth = null;
